macro(sqlite_test target file)
  simdb_test(${target} ${file})
  target_include_directories(${target} PRIVATE ${SIMDB_BASE}/test/sqlite)
endmacro()

add_subdirectory(Schema)
add_subdirectory(Insert)
add_subdirectory(Delete)
add_subdirectory(Query)
add_subdirectory(UInt64)
